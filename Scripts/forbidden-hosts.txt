# Phase 5.4 — hosts that must never appear in a LingyueAppStore build.
#
# A match here against the App Store .app bundle (main binary, bundled
# resources, or Assets.car metadata) is a structural regression: the
# compile-time fork already guarantees LingyueInternalSources is not
# linked, so a hit means a developer hard-coded one of these strings
# directly in shared (App Store-linked) source.
#
# Comments (`#`) and blank lines are ignored. One host per line, no
# protocol prefix, no path.
#
# Update this list whenever a new seeded source lands in
# LingyueInternalSources, or when a new fast-path adapter is added to
# BookImportService.

# Seeded rule homepages
52shuku.net
51shucheng.net
nunucom.com
powanjuan.cc
sto9.com
tongrenquan.org
trxs.org
xbanxia.cc
xsw.tw
zhswx.com

# Legacy host-pattern catalog (Novel.BookSourceRegistry.sources, Internal only)
bqgl.cc
daweixs.com
bq99.cc
esjzone.cc
5dxs.net
adxs.net
uuks.org
hjwzw.com
lnovel.org
piaotian8.com
69shuba.com

# Fast-path adapter hosts (BookImportService biqugeAPIHosts + helpers)
apiqu.cc
apige.cc
8book.com
