import SwiftUI

// Placeholder. The dev-only reader-diagnostics surface has been removed;
// `ReaderDiagnostics` (the data layer) is still used for crash-log
// breadcrumbs but no longer has a user-visible view in the App Store
// build.
