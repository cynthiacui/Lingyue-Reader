import XCTest
@testable import LingyueInternalSources

final class SeededRuleLoaderTests: XCTestCase {
    // Placeholder — original tests covered the dev-only seeded-rule loader,
    // which has been removed.
}
