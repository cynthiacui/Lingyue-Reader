import XCTest
@testable import LingyueInternalSources

final class InternalSourceRegistryTests: XCTestCase {
    // Placeholder — original tests covered the dev-only registry, which has
    // been removed.
}
