import XCTest
@testable import LingyueInternalSources

final class Phase2RegistryProbeTests: XCTestCase {
    // Placeholder — original probe tests covered the dev-only registry,
    // which has been removed.
}
