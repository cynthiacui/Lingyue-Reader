import Foundation

// Module retained as a build-system placeholder so the existing project
// configuration remains valid; its dev-only seeded-rule loading is no
// longer wired into either build target.
