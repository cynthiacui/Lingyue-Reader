import XCTest
@testable import LingyueCore

final class SourceImportRoundTripTests: XCTestCase {
    // Placeholder — original end-to-end test loaded a bundled rule JSON
    // that is no longer shipped from this repo.
}
