import XCTest
@testable import LingyueCore

final class Phase2SeededRuleTests: XCTestCase {
    // Placeholder — original tests validated specific seeded rules; both
    // the rules and the captured-HTML fixtures have been removed.
}
