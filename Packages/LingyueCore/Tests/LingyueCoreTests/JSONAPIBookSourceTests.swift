import XCTest
@testable import LingyueCore

final class JSONAPIBookSourceTests: XCTestCase {
    // Placeholder — original tests exercised the JSON-API engine with
    // shape-specific fixtures; replace with fresh shape-agnostic coverage
    // when needed.
}
