import XCTest
@testable import LingyueCore

final class Phase25PrototypeTests: XCTestCase {
    // Placeholder — original prototype tests drove the rule engine through
    // captured HTML from specific sites; that fixture set has been removed.
}
